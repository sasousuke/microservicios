version: '3.5'

services: 
     registry:
        image: registry:3
        container_name: registry
        restart: always
        ports: 
            - 5000:5000
        volumes:
            - /opt/registry/data:/var/lib/registry
